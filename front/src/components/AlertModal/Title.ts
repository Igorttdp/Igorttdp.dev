import { AlertDialogTitle } from "@radix-ui/react-alert-dialog";
import styled from "styled-components";

const AlertTitle = styled(AlertDialogTitle)`
    font-size: 2rem;
`

export default AlertTitle
