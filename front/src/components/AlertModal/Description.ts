import { AlertDialogDescription } from "@radix-ui/react-alert-dialog";
import styled from "styled-components";

const AlertDescription = styled(AlertDialogDescription)`
  margin: 2rem 0;
  font-size: 1.4rem;
`;

export default AlertDescription;
